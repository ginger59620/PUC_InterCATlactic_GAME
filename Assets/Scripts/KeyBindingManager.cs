using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class KeyBindingManager : MonoBehaviour
{
   /* [Header("Objects")]
    [SerializeField] private TextMeshProUGUI buttonOne;

    private void Start()
    {
        
    }
    private void Update()
    {
        if (buttonOne.text == "Awaiting Input")
        {
            foreach(KeyCode keycode in EnumButtonsAttribute)
        }
    }*/
}
